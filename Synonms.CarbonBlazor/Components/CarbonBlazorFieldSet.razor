<fieldset class="cb-field-set @(IsDoubleColumn ? "double" : string.Empty)" readonly="@IsReadOnly" disabled="@IsDisabled">
    @ChildContent
</fieldset>

@code {

    [Parameter]
    [EditorRequired]
    public RenderFragment ChildContent { get; set; } = null!;

    [Parameter]
    public bool IsDisabled { get; set; } = false;

    [Parameter]
    public bool IsReadOnly { get; set; } = false;

    [Parameter]
    public bool IsDoubleColumn { get; set; } = false;
}